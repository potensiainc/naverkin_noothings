// Answer writing is performed by Claude Code Runtime Agent (see prompts/daily-run.md).
// This module is intentionally empty — do not add template or extraction logic here.
//
// The Claude Code Agent reads each question and article directly, then writes
// a contextual answer. There is no programmatic answer generation in TypeScript.

export {};
